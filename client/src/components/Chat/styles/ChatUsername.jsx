import styled from 'styled-components';

const ChatUsername = styled.h1`
  display: flex;
  font-weight: normal;
  font-size: 1.1rem;
  flex: 1 1 auto;
  padding-left: 1rem;
`;

export default ChatUsername;
