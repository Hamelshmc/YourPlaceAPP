import styled from 'styled-components';

export const WelcomeContainer = styled.main`
  filter: drop-shadow(0 0 5px #0003), blur(5px);
  display: flex;
  justify-content: center;
  position: relative;
  height: 15rem;
`;
