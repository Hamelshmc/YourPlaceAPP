import styled from 'styled-components';

const TittleWelcome = styled.h1`
  position: absolute;
  font-size: 1.5rem;
  text-align: center;
  letter-spacing: 0.25rem;
  text-transform: uppercase;
`;

export default TittleWelcome;
